  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h4>Failed to post this comment</h4>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for(@comment) do |f| %>

<div class="row">
  <div class="field col-md-9">
    <label class="sr-only">Comment:</label>
    <%= f.text_area :content,rows:1, placeholder: "Comment on your friend's post...", :class => "form-control" %>
  </div>

   <div class="field">
    <%= f.hidden_field :post_id, :value => params[:post_id]%>
  </div>

    <div class="field">
      <%= f.hidden_field :commenttype, :value => params[:commenttype] %>
  </div>

  <div class="actions col-md-1">
    <%= f.submit "Submit", :class => 'btn btn-primary'%>
  </div>
</div>

<% end %>

<%= form_for(@unreadcomment) do |f| %>
  <% if @unreadcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unreadcomment.errors.count, "error") %> prohibited this unreadcomment from being saved:</h2>

      <ul>
      <% @unreadcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
